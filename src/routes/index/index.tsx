import classes from './index.module.css';

export default function Index() {
  return (
    <section className={classes.container}>
      <h1 className={classes.title}>Select an episode...</h1>
    </section>
  );
}
